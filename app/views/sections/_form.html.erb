<%= form_for @section, :remote => true do |f| %>
  <div class="modal-body">
    <div class="error_explanation">
    </div>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name, class: "form-control input-lg" %>
    </div>
    <div class="field">
      <%= f.label :batch %><br>
      <%= f.select :batch_id, options_for_select(Batch.all.collect {|p| [ p.name, p.id ] },(f.object.try(:batch_id))),{include_blank: 'None'}, { :class => 'form-control input-lg'} %>
    </div>
  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-lg btn-default" data-dismiss="modal">Close</button>
    <%= f.submit "Save Subject", class: "btn btn-lg btn-primary" %>
  </div>
<% end %>