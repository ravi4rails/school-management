var options = '<option value="">Select Batch</option>';
<% @batches.each do |batch|%>
options += '<option value="<%= batch.id%>"><%= batch.name%></option>'
<%end%>
$("#find_student_batch").html(options);