<% if @batch.errors.any? %>
  $(".error_explanation").html("<%= escape_javascript(render('batches/error'))%>")
<% else %>
  $("#editBatch").modal('hide')
  $('#batch_<%= @batch.id %>').replaceWith("<%= escape_javascript(render('batch', batch: @batch )) %>")
<% end %>