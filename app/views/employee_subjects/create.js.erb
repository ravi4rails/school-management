<% if @employee_subject.errors.any? %>
  $(".error_explanation").html("<%= escape_javascript(render('employee_subjects/error'))%>")
<% else %>
  $("#newEmployeeSubject").modal('hide')
  $("#employee-subject-index").append("<%= escape_javascript(render('employee_subject', employee_subject: @employee_subject )) %>")
<% end %>