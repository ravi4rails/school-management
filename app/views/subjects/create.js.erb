<% if @subject.errors.any? %>
  $(".error_explanation").html("<%= escape_javascript(render('subjects/error'))%>")
<% else %>
  $("#subject-modal").modal('hide')
  $("#subject-index").append("<%= escape_javascript(render('subject', subject: @subject )) %>")
<% end %>